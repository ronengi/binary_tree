static int find_command(input_str);

find_command->shrink_n

מחפשת פקודה מטבלת הפקודות בתוך input_str.

מקבלת:
	מחרוזת.

	כאשר היא מוצאת פקודה היא מכווצת את input_str
	לדרוס את הפקודה.

מחזירה:
	אינדקס לפקודה המתאימה בטבלת הפקדות.
	1- אם לא נמצאה התאמה.
