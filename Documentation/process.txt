static int process(char *input_str);

process->find_command

תופעל כאשר מחסנית הפקודות ריקה.

מקבלת:
	מחרוזת.

פעולה:
	מוציאה מתחילתה פקודות לפי טבלת הפקודות.
	מכווצת את המחרוזת המקורית.
	שמה את הפקודות במחסנית.

מחזירה:
	מספר הפקודות שעיבדה.
	1- אם יש שגיאה.
